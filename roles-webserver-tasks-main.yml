- name: Install Apache
  dnf:
    name: httpd
    state: present

- name: Enable Apache
  systemd:
    name: httpd
    state: started
    enabled: yes

- name: Deploy web page
  template:
    src: index.html.j2
    dest: /var/www/html/index.html
  notify: restart apache
